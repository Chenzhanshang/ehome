<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyct.ehome.dao.CommitteeMapper">
    <select id="selectCommitteeByCommunityId" parameterType="String" resultType="Committee">
        select * from t_committee where community_id = #{communityId}
    </select>
</mapper>